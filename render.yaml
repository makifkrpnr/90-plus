services:
  - type: web
    name: 90-plus
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: npm install --omit=dev --no-package-lock --registry=https://registry.npmjs.org/ && npm ls socket.io
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: commit
